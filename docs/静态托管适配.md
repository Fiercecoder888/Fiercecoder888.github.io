# 静态托管适配审计（子路径部署 / GitHub Pages 项目站）

> 本文是对 `docs/部署.md` §1.2 中「项目站的正确修法见 `docs/静态托管适配.md`」那一段的落实。
> 基础设施层面的结论（仓库没配 `app.baseURL`、工作流没传 base）见姊妹文档 `docs/静态产物验证.md` §「问题 1」；
> 本文负责**逐文件清点硬编码根路径**，并给出每一条的修法。

> **状态更新（阅读本文前请先看这 3 条）**
> 1. 本文写于「还没决定部署形态、工作流仍依赖 `actions/configure-pages`」的时期。现在这两件事都定了：仓库已重命名为 `Fiercecoder888.github.io`，**最终部署形态是用户站**，站点落在域名根 `https://fiercecoder888.github.io/`，`app.baseURL` 保持 `/`。
> 2. 因此 §1/§2 里「54 处根绝对路径必须改」**在当前形态下不成立**——这些以 `/` 开头的路径现在**是正确的**。这份清单作为**「假设要走项目站」的备选路线资料**保留，不是当前现状，也不需要照做。
> 3. 工作流已**彻底移除 `actions/configure-pages@v5`**，`steps.pages.outputs.base_url` / `steps.pages.outputs.base_path` **都不存在了**。文中凡引用这两个 output 的地方均已就地修正为现状写法（见 §5 第 2 步），凡建议传 `NUXT_APP_BASE_URL` 的地方已标注**已废弃 / 不再需要**。

## 一句话结论

**用户站（仓库名 `<user>.github.io`，站点在 `/`）现状零改动即可用；项目站（站点在 `/<repo>/`）必须先把 `app.baseURL` 配起来，再改掉 54 处以 `/` 开头的硬编码路径——否则桌面图标、壁纸、RSS/Sitemap/llms.txt 入口会集体 404。**

**本站最终选的就是前半句**：仓库名 `Fiercecoder888.github.io`、站点在域名根，所以那 54 处一处都不用改；后半句属于「假设要走项目站」的备选路线，不是当前现状。

补一句容易反直觉的：`app/assets/css/main.css` 里那 10 条 `url('/cursors/...')` **看起来是最典型的硬编码，实际是安全的**——构建时会被 Nuxt/Vite 改写成相对路径 `url(../cursors/...)`，天然与 base 无关（证据见 §4.2）。`docs/部署.md:70` 说这 10 条会坏，这一条**已证伪**，请以本文为准。

## 0. 审计范围与证据

- 审计对象：工作区 `my-blog/` 的全部源码（`app/`、`server/`、`shared/`、`nuxt.config.ts`、`.github/workflows/`），`git HEAD = ad88041`（工作区干净）。**注**：这是**审计当时**的快照——审计之后 `HEAD` 已前进到 `8787cc3`，且工作区存在未提交改动（`TopBar.vue`、`docs/部署.md`、`docs/静态产物验证.md`、`package.json`、`tools/gh-status.mjs`），所以下文行号对**审计当时**准确，对当前工作区可能已漂移，引用前建议重新 grep 一次。
- 方法：`grep` 全量扫描 + 逐文件 `read` 复核行号 + 对**已有构建产物 `dist/`（`dist/` 是上一次 `pnpm generate` 的产物）**做交叉验证。
- 行号全部来自真实 grep/read 结果。
- **未运行** `pnpm generate` / `pnpm build` / `pnpm install`；因此「把 base 设成 `/repo/` 之后产物长什么样」没有实测，只做静态推理。凡属推理的地方都标了「推理」。

## 1. 现状总览：66 处引用，分成三类

| 类别 | 数量 | 项目站下的结果 | 处置 |
| --- | --- | --- | --- |
| 会坏、必须改 | **54** | 404 / 图标裂 / 壁纸空白 | §2 表格逐条列出 |
| 另一类问题（与 base 无关） | 2 | 静态托管本来就没有后端 | §3 |
| 形似硬编码、实际安全 | 10 | 正常 | §4.2 已验证 |

> **前提提醒**：上表「会坏、必须改」一列、以及 §2 整张逐条清单，都是**在项目站（站点带 `/<repo>/` 子路径）前提下**得出的。本站最终走的是用户站（站点在域名根），所以那一列在当前形态下应读作「**若改走项目站则会坏**」——现状一个字都不用改。清单本身的价值不变（它同时是「哪些路径是根绝对的」的完整地图）。

54 处按类别小计：

| 子类 | 数量 | 说明 |
| --- | --- | --- |
| `/app-icons/...` 图标路径 | 21 | Dock 10 + 启动台 11 |
| `/wallpapers/...` 壁纸 | 8 | 全部壁纸 |
| `/rss.xml` `/sitemap.xml` `/llms.txt` 硬链接 | 21 | 页脚 / 首页 / 菜单栏 / Finder / 关于 / Dock / 启动台 / `<head>` |
| `AboutWindow.vue` 里裸 `<a>` 的站内页面链接 | 3 | `/blog` `/tags` `/about`（**最易漏的一类**） |
| `<head>` 里的 `/favicon.svg` | 1 | 站点图标 |

## 2. 逐条清单（项目站前提下「会坏、必须改」）

优先级说明：
- **P0** = 项目站下肉眼可见地坏掉，必须改。
- **P1** = 也会坏，但更隐蔽（容易被漏，或只在特定入口出现）。
- **P2** = 与 base 无关的另一类问题。
- **本站现状** = 本节所有「建议修法」都**只在改走项目站时才需要执行**。当前是用户站、站点在域名根，下面 54 处根绝对路径**全部是正确的**，一处都不要动。

| 文件:行号 | 片段 | 影响 | 建议修法 | 优先级 |
| --- | --- | --- | --- | --- |
| `nuxt.config.ts:39` | `{ rel: 'icon', type: 'image/svg+xml', href: '/favicon.svg' }` | 站点图标请求打到域名根，404（标签页无图标）。**注**：`app.head` 不会被 Nuxt 自动加 base（见 §4.4） | 在 `nuxt.config.ts` 顶部算一个 `const base = process.env.NUXT_APP_BASE_URL \|\| '/'`，写成 `href: \`${base}favicon.svg\``（配置文件里不能用 composable，只能用环境变量/常量） | P0 |
| `nuxt.config.ts:40` | `{ rel: 'alternate', type: 'application/rss+xml', title: 'RSS', href: '/rss.xml' }` | RSS 自动发现链接失效（阅读器/浏览器订阅按钮指向域名根） | 同上，`href: \`${base}rss.xml\`` | P0 |
| `app/configs/apps.ts:24,31,38,46,54,62,70,77,84,92` | `icon: '/app-icons/finder/128.png'`（及 launchpad / safari / notes / contacts / terminal / system-preferences / wallpapers / news / trash 共 10 条） | Dock 与启动台的所有 App 图标全部裂图（`AppDock.vue:16` → `DockItem.vue:13-14` 直接 `<img :src="iconSrc">`，字符串原样进 DOM）。实测产物里就是绝对路径（§4.1） | 二选一：① 在 `AppDock.vue` 分发前用 `joinURL(useRuntimeConfig().app.baseURL, app.icon)` 拼；② 把 `icon` 改存**不含前导斜杠**的相对值，再用 `publicAssetsURL('app-icons/finder/128.png')`（`#internal/nuxt/paths`，自动含 base） | P0 |
| `app/configs/apps.ts:86` | `to: '/rss.xml'`（`kind: 'link'`） | Dock 的「RSS 订阅」图标经 `AppDock.vue:17` 变成裸 `<a href="/rss.xml">`（`DockItem.vue:3-5`），点击 404 | 该字段是链接不是路由：在组件层拼 `joinURL(baseURL, app.to)` 后再传给 `href` | P0 |
| `app/configs/wallpapers.ts:19,25,31,37,43,49,55,61` | `background: 'url("/wallpapers/big-sur-1.jpg") center / cover no-repeat fixed'`（big-sur-1/2/3/4、big-sur-graphic-1、monterey-1/2/3 共 8 条） | **桌面壁纸整块空白**（`WallpaperLayer.vue:7` 以 `:style="style"` 内联下发，走 `useWallpaper()`）。内联样式是纯 JS 字符串，构建期任何 CSS url 改写都不会碰到它。实测产物里仍是 `url("/wallpapers/big-sur-1.jpg")`（§4.1） | 在 `app/composables/useWallpaper.ts` 里拼：把配置值存成 `/wallpapers/xxx.jpg`（去掉 CSS 外壳）或用占位符，真正下发时 `background: \`url("${joinURL(baseURL, path)}") center / cover ...\`` | P0 |
| `app/components/desktop/LaunchpadOverlay.vue:76,77,78,79,80,81,82,83,84,85,86` | `src: '/app-icons/finder/128.png'` 等 11 条 | 启动台（Launchpad）网格里的 App 图标全部裂图（模板 `LaunchpadOverlay.vue:35-41` 的 `<img :src="app.src">`） | 同 `apps.ts` 的图标修法，统一在一处拼 base | P0 |
| `app/components/desktop/LaunchpadOverlay.vue:85,89` | `to: '/rss.xml'`、`to: '/sitemap.xml'`（`kind: 'link'`） | 启动台点「RSS 订阅 / Sitemap」时 `window.open(app.to)`（`LaunchpadOverlay.vue:106`）打开的是域名根的 404 页 | 拼 `joinURL(baseURL, app.to)`；或用 `<NuxtLink>`（非路由资源建议保持 `<a>`/`window.open` 并显式拼 base） | P0 |
| `app/layouts/default.vue:21,23,25` | `<a href="/sitemap.xml">`、`<a href="/llms.txt">`、`<a href="/rss.xml">` | 全站页脚的三个链接 404（裸 `<a>`，Nuxt 不会加 base） | 改成 `<a :href="joinURL(baseURL, '/sitemap.xml')">`（用 `<a :href>` 而非 `<NuxtLink>`，避免被客户端 router 接管去解析一个不存在的页面路由） | P0 |
| `app/pages/index.vue:28,31,34` | `<a href="/rss.xml" title="RSS">`、`/llms.txt`、`/sitemap.xml` | 首页右上角三个图标按钮 404 | 同上 | P0 |
| `app/components/desktop/AboutWindow.vue:48,49,50` | `{ label: 'RSS', href: '/rss.xml' }`、`/sitemap.xml`、`/llms.txt` | 「关于本站」窗口的站点索引按钮 404（模板 `AboutWindow.vue:22` 是裸 `<a :href="link.href">`） | 同上 | P0 |
| `app/components/desktop/FinderWindow.vue:140,141,142` | `window.open('/rss.xml', '_blank')`、`/sitemap.xml`、`/llms.txt` | Finder 的「pages」视图里三个入口 404（同一行的 `subtitle` 只是展示文案，可一并改成带 base 的形式以保持一致） | `window.open(joinURL(baseURL, '/rss.xml'), '_blank')` | P0 |
| `app/components/desktop/TopBar.vue:299,300,301,314,315` | `openExternal('/rss.xml')`、`/sitemap.xml`、`/llms.txt`（Go 菜单）、`/llms.txt`、`/rss.xml`（Help 菜单） | 顶部菜单栏「前往 / 帮助」里的 5 个入口 404（`TopBar.vue:363-366` 的 `openExternal` 是 `window.open(path)`） | 在 `openExternal` 内部统一拼 base（一处改动覆盖这 5 条） | P0 |
| `app/components/desktop/AboutWindow.vue:45,46,47` | `{ label: '文章', href: '/blog' }`、`{ label: '标签', href: '/tags' }`、`{ label: '关于', href: '/about' }` | **最易漏的一类**：这三条虽然是**站内页面路由**，但用的是**裸 `<a href>` 全页跳转**，不走 vue-router，所以一样会跳到域名根 → 404。`docs/部署.md` 只列了 48-50 行，漏了这三条 | 这三条改成 `<NuxtLink :to="link.href">`（NuxtLink 会自动带 base，见 §4.3）；或同样拼 base | P1 |
| `shared/site.ts:37` | `url: 'https://fiercecoder888.github.io'`（**已核实更新**：原审计时写的是 `https://fiercecoder.github.io`——那个域名不是本人的，真实账号是 `Fiercecoder888`，首字母大写 F） | 该值是 `runtimeConfig.public.siteUrl` 的兜底（`nuxt.config.ts:26`），会进 `sitemap.xml` / `rss.xml` / `llms.txt` / `<link rel="canonical">`。现在它写的**就是真实用户站地址（域名一律小写）**，与实际部署位置一致；只有「假设走项目站」时才会少一段 `/my-blog` | **原建议已作废**：「不要在源码里写死域名、本地兜底留 `http://localhost:3000`、部署一律由 `NUXT_PUBLIC_SITE_URL` 覆盖」是审计当时（形态未定）的取向。现状相反——`shared/site.ts` 里放的就是**真实域名**，`nuxt.config.ts:26` 用 `process.env.NUXT_PUBLIC_SITE_URL \|\| SITE.url` 兜底，所以本地构建不设环境变量时写出的也是正确域名，不是 localhost。**仅当**将来真做项目站，兜底值才需要改成带子路径的地址（`https://<账号>.github.io/<repo>`） | P1 |
| `.github/workflows/deploy.yml` 的 `Generate static site` 步骤（审计时在 `:41-45`，当前版本在 `:50-54`） | 现状写法是 `run: pnpm generate` + `env: NUXT_PUBLIC_SITE_URL: ${{ steps.site.outputs.url }}`（只传了 site URL；`steps.site.outputs.url` 由上面的 `Resolve site url` 步骤从账号名推导，见 §5 第 2 步） | **（项目站前提下成立）一切改法的前提**：只把 `NUXT_PUBLIC_SITE_URL` 设成 `…/my-blog` 只能修正 sitemap/RSS/llms 里**写在文字里的**绝对地址，对静态资源请求和硬编码链接**一个都修不好**。仓库没有 `app.baseURL`，所以 `_nuxt/` 资源与所有 `/xxx` 仍然是根绝对。**注**：这条结论是在「项目站」前提下得出的；本站走用户站，站点就在域名根，所以现状无需处理 | **原建议「补一行 `NUXT_APP_BASE_URL: ${{ steps.pages.outputs.base_path }}/`」已废弃 / 不再需要**：(1) `actions/configure-pages` 已从工作流移除，`steps.pages.outputs.base_path` 这个 output **不存在**了；(2) 站点形态定为**用户站**，落在域名根 `https://fiercecoder888.github.io/`，`baseURL` 保持 `/`，「项目站带一层 `/my-blog` 子路径」这个前提整个消失，那些根绝对路径反而是正确的。**如果将来真的要用项目站**（仓库名 ≠ `<账号>.github.io`），`base_path` 不能再从 `configure-pages` 拿，得**自己推导**（例如 `NUXT_APP_BASE_URL: /${{ github.event.repository.name }}/`，Nuxt 需要尾斜杠），并同步把 `shared/site.ts:37` 的兜底值改成带子路径的地址 | P0（**仅项目站**） |

## 3. 另一类问题：静态托管下没有后端（与 base 无关）

| 文件:行号 | 片段 | 影响 | 建议修法 | 优先级 |
| --- | --- | --- | --- | --- |
| `app/components/BlogComments.vue:157,180` | `await $fetch('/api/comments', {...})` | `pnpm generate` 产出的是纯静态站，Nitro 的 `/api/comments`（`server/api/comments/index.get.ts`）**根本不存在**，请求必然 404。这与子路径无关——用户站同样会坏。**注意别误判**：Nuxt 的全局 `$fetch` 是用 `baseURL: app.baseURL` 初始化的（`nuxt/dist/index.js:3237-3240`，ofetch 会 `withBase`），所以这两处在子路径下**不会指错地址**，坏的原因是「没有后端」 | **已完成**：「评论不可用」的降级 UI 已经实现在 `app/components/BlogComments.vue` 里（`:20-22` 的说明块、`:133` 的注释、`:164-165` 对 404/405 的判定），404 不会冒泡成报错。所以本条从「待办」降级为「已处理，保留作为结论记录」——结论本身与 base 无关，用户站/项目站都一样 | P2 |

## 4. 已确认无需修改的部分

### 4.1 有构建产物实测证据的三类

`dist/`（上一次以 base `/` 生成的产物）里：

| 类别 | 源文件里的写法 | 产物里的实际形态 | 结论 |
| --- | --- | --- | --- |
| 光标 | `main.css:539` `url('/cursors/normal-select.svg')` | `dist/_nuxt/entry.Bv2SmSWs.css` 里是 **`url(../cursors/normal-select.svg)`** | **相对路径，与 base 无关 → 反过来在项目站下也对** |
| 壁纸 | `wallpapers.ts:19` `url("/wallpapers/big-sur-1.jpg")` | 产物 JS 里仍是 **`url("/wallpapers/big-sur-1.jpg")`**（8 张全部如此） | 根绝对，未改写 → **必须改** |
| 图标 | `apps.ts:24` `'/app-icons/finder/128.png'` | 产物 JS 里是 **`/app-icons/finder/128.png`**（10 个图标全部如此） | 根绝对，未改写 → **必须改** |
| 根路径链接 | `TopBar.vue:299` `openExternal('/rss.xml')` 等 | 产物 JS 里是 **`/rss.xml`、`/sitemap.xml`、`/llms.txt`** | 根绝对，未改写 → **必须改** |
| `<head>` | `nuxt.config.ts:39,40` | `dist/index.html` 里是 `<link rel="icon" … href="/favicon.svg">`、`<link rel="alternate" … href="/rss.xml">` | 根绝对，未改写 → **必须改** |

> 本表「结论」列里的「必须改」同样是**项目站前提**下的结论。本站走用户站，站点就在域名根，这些根绝对字符串**正是想要的结果**，无需改动。（本表的实测证据本身与形态无关：它证明的是「这些字符串不会被构建期改写成相对路径」。）

### 4.2 为什么 `main.css` 的 `url('/cursors/...')` 是安全的（与 `docs/部署.md:70` 相反）

结论不是猜的，有两条独立机制 + 产物实测：

1. **产物实测**：`dist/_nuxt/entry.Bv2SmSWs.css` 中 10 条光标全部是 `url(../cursors/*.svg|webp)`。
2. **Vite 侧机制**：CSS 里的 `url()` 若命中 `public/` 下的真实文件，会走 `checkPublicFile` → `publicFileToBuiltUrl` 变成占位符，最后经 `toOutputFilePathInCss` 落地；而 Nuxt 给 Vite 注入了 `experimental.renderBuiltUrl`，对非 JS host 明确返回相对路径：
   `@nuxt/vite-builder@4.5.2/dist/index.mjs:2600-2601` → `renderBuiltUrl: (filename, { type, hostType, ssr }) => { if (hostType !== "js") return { relative: true }; … }`
   → CSS（`hostType === 'css'`）拿到 `{ relative: true }`，由 Vite 输出成相对 CSS 文件位置的 `../cursors/...`。
3. **Nuxt 侧机制（对带引号的 url 不生效，但可作旁证）**：`@nuxt/vite-builder/dist/index.mjs:737-748` 的 `generateBundle` 会把 CSS 里的 `url(/public-asset)` 改写成相对路径。注意其正则是 `CSS_URL_RE = /url\((\/[^)]+)\)/g`（同文件 `:693`）——**要求 `(` 后紧跟 `/`，所以只匹配不带引号的 `url(/x)`**。本项目的写法是 `url('/cursors/...')`（带单引号），这条**不匹配**，真正生效的是上面第 2 条。
   ——这一点值得记下来：同一份 CSS 如果把引号去掉写成 `url(/cursors/x.svg)`，就会走第 3 条；写成 `url('/cursors/x.svg')` 走第 2 条。两条都通向相对路径，所以**带不带引号都安全**。

前提：引用的文件必须真实存在于 `public/` 下。已核对，这 10 个文件在 `public/cursors/` 中全部存在（`normal-select.svg`、`link-select.svg`、`text-select.svg`、`move.svg`、`busy.webp`、`unavailable.svg`、`diagonal-resize-1.svg`、`vertical-resize.svg`、`diagonal-resize-2.svg`、`precision-select.svg`）。任一文件缺失，就会退回「构建期不解析、运行时原样保留」从而**恢复成根绝对路径**并真的坏掉。

> **2026-09-18 更新：引用数从 10 条变成 11 条。** 修自定义光标热点时发现上游没有「↔ 水平缩放」那张图，于是新增了派生的 `horizontal-resize.svg`（由 `scripts/derive-cursors.mjs` 把 `vertical-resize.svg` 绕中心旋转 -90° 生成），并给全部 11 条补上了热点坐标。已在**新的构建产物**上复核：11 条全部是 `url(../cursors/...) <x> <y>` 相对路径，`url('/cursors/...')` 残留 **0** 处，线上同样如此。本文其余处写的「10 条」是 2026-09-18 之前的读数，结论不受影响 —— 新增那条同样满足「文件真实存在于 `public/` → 构建期改写成相对路径」这个前提。

### 4.3 `<NuxtLink>` 和 `router.push` 自动带 base

这两类是**正确写法，不要改**。源码依据（`nuxt@4.5.2`）：

- `<NuxtLink>` 的 href 生成：`nuxt/dist/app/components/nuxt-link.js:96` → `applyTrailingSlashBehavior(joinURL(config.app.baseURL, to.value), …)`
- `router.push` 的地址写入：`nuxt/dist/app/plugins/router.js:70` → `joinURL(baseURL, to.fullPath)`
- 命令式跳转：`nuxt/dist/app/composables/router.js:153` → `joinURL(useRuntimeConfig().app.baseURL, fullPath)`

据此**无需修改**的具体位置（这些是路由 `to` / `router.push`，不是 `<a href>`）：

- `app/error.vue:25,31`（`<NuxtLink to="/blog"｜"/tags">`）、`:55` 的 `clearError({ redirect: '/' })`
- `app/components/desktop/MacWindow.vue`：**全文没有任何根路径**；只有 `closeTo: '/'` 默认值（`:80`）经 `router.push(props.closeTo)`（`:115`）使用，以及 `route.fullPath` 展示（`:50`）
- `app/layouts/default.vue:65` 的 `route.path.startsWith('/blog/')` —— 这是**运行时对路径的字符串判断**（判断当前是否在文章详情页），不是要请求的 URL，与部署位置无关，正确
- `app/components/desktop/DesktopLayer.vue:65`（`router.push('/blog')`）
- `app/components/desktop/FinderWindow.vue:130,137,138,139,159`（`router.push(...)`）
- `app/components/desktop/TopBar.vue:264,265,266,295,296,297`（`navigate()` → `useRouter().push`，见 `TopBar.vue:358-361`）
- `app/configs/apps.ts:40,48,56`（`to: '/blog'｜'/tags'｜'/about'`，`kind: 'route'`，经 `AppDock.vue:72` 的 `router.push(app.to)`）
- `app/components/desktop/LaunchpadOverlay.vue:78,79,80,88`（`kind: 'route'`，经 `:107` 的 `router.push(app.to)`）
- `app/composables/useBlogSearch.ts:111`（`path` 兜底 `'/blog'`，由 `SpotlightOverlay.vue:64,118` 的 `go(hit.path)` 交给 router）
- `app/pages/index.vue:37,61,124,131,143`、`app/pages/blog/[...slug].vue:22,64`、`app/pages/tags/index.vue:12`、`app/pages/tags/[tag].vue:4`（全部 `<NuxtLink>`）
- `app/components/desktop/TerminalWindow.vue:149` 的 `print('/home/guest/blog')` —— 只是终端里打印的**文案**，不是 URL

### 4.4 `<head>` 需要手工处理（不是「Nuxt 会自动加 base」）—— **仅项目站**

已核实 `nuxt@4.5.2` 与 `@unhead/vue@3.4.0` 的 head 运行时里**没有任何 `baseURL` 处理**（对 `nuxt/dist/head/**`、`nuxt/dist/app/composables/head.js`、`@unhead/vue/dist` 检索 `baseURL` 均无命中），产物 `dist/index.html` 也确认 `href="/favicon.svg"`、`href="/rss.xml"` 原样输出。所以**如果走项目站**，`nuxt.config.ts:39,40` 必须自己拼 base。**注**：本站走用户站，这两条根绝对路径正好指向域名根下的真实文件，**原样保留就是对的**，不要照这条去改。

### 4.5 服务端路由：拼的是完整 URL，**无需修改**

`server/routes/` 下 4 个文件都从 `runtimeConfig.public.siteUrl` 取站点前缀再拼绝对地址，这是正确做法（`siteUrl` 是该站点的**完整前缀**。本站现状是用户站，工作流推导出 `https://fiercecoder888.github.io`（**不带子路径**）；**只有在项目站下**它才会带上 `/<repo>/` 这一段）：

| 文件:行号 | 片段 | 说明 |
| --- | --- | --- |
| `server/routes/sitemap.xml.ts:3` | `const site = String(config.public.siteUrl \|\| '').replace(/\/$/, '')` | 取 `siteUrl` 并去掉尾斜杠 |
| `server/routes/sitemap.xml.ts:11-14` | `{ loc: \`${site}/\`, … }`、`\`${site}/blog\``、`\`${site}/tags\``、`\`${site}/about\`` | 完整绝对 URL ✅ |
| `server/routes/sitemap.xml.ts:18` | `loc: \`${site}${post.path}\`` | 完整绝对 URL ✅ |
| `server/routes/rss.xml.ts:15,16` | `<link>${site}${post.path}</link>`、`<guid isPermaLink="true">…` | 完整绝对 URL ✅ |
| `server/routes/rss.xml.ts:24,28` | `<link>${site}</link>`、`<atom:link href="${site}/rss.xml" …>` | 完整绝对 URL ✅ |
| `server/routes/llms.txt.ts:21-26` | `- 首页：${site}/` … `- Sitemap：${site}/sitemap.xml` | 完整绝对 URL ✅ |
| `server/routes/llms.txt.ts:30` | `[${post.title}](${site}${post.path})` | 完整绝对 URL ✅ |
| `server/routes/robots.txt.ts:6,29,30` | `Sitemap: ${site}/sitemap.xml`、`LLMs: ${site}/llms.txt` | 完整绝对 URL ✅ |

这些是**运行时拼接**，与静态产物的资源路径无关，因此（在项目站下）同样正确——**前提是 `NUXT_PUBLIC_SITE_URL` 传对了**。现状写法：工作流的 `Resolve site url` 步骤由**账号名推导**站点地址（`echo "url=https://$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]').github.io"`），再作为 `NUXT_PUBLIC_SITE_URL` 传给 `Generate static site`。本站该值即 `https://fiercecoder888.github.io`（域名小写、**无子路径**）。

> 旧文此处写的是「工作流已传 `steps.pages.outputs.base_url`，项目站自带 `/my-blog`」——**已作废**：`actions/configure-pages@v5` 已从工作流彻底移除，`steps.pages.outputs.*` 这些 output 不存在了。移除原因是实测它即使加 `enablement: true` 也无法创建 Pages 站点，报 `HttpError: Resource not accessible by integration` / `Create Pages site failed. Error: Resource not accessible by integration` —— `POST /repos/{owner}/{repo}/pages` 不在 `GITHUB_TOKEN` 的权限范围内，需要 PAT。现在的写法由账号名推导（用户站地址固定是 `https://<账号>.github.io`），**不再依赖 Pages 是否已启用**，所以 build 层即使 Pages 没开也能单独跑通。必须用 `tr` 转小写，因为 GitHub Actions 表达式**没有** `toLowerCase`，不转会写出 `https://Fiercecoder888.github.io`（大写 F），与 `shared/site.ts` 不一致，canonical/sitemap/RSS 会被 SEO 当成两个 URL。

### 4.6 其它已确认安全的部分

- `#internal/nuxt/paths` 提供的 `buildAssetsURL()` / `publicAssetsURL()` 已经带上 base：`nuxt/dist/index.js:3207-3213` → `publicAssetsURL()` 返回 `appConfig.cdnURL || appConfig.baseURL`；`_nuxt/` 资源与 payload 请求都走它。所以**只要配好 `app.baseURL`，`_nuxt/` 静态资源不需要手工处理**（属推理，未实测）。
- `.github/workflows/deploy.yml` 的 `Disable Jekyll` 步骤（`touch .output/public/.nojekyll`；审计时位于 `:48-49`，当前版本位于 `:57-58`）：正确，能避免 GitHub Pages 的 Jekyll 忽略 `_nuxt/`（下划线开头）目录。
- `nuxt.config.ts:62` 的 prerender 路由表 `/`、`/about`、`/blog`、`/tags`、`/rss.xml`、`/sitemap.xml`、`/llms.txt`、`/robots.txt`：这些是 Nitro 侧的路由名，会随 `app.baseURL` 一起挂到子路径下，写法正确。

## 5. 如果要做项目站，最小改动清单（按顺序）

> **先明确**：这是**备选路线**，本站**没有**采用。当前部署形态是用户站（站点在域名根），下面第 1、2 步里的 `base` / `NUXT_APP_BASE_URL` 部分**已废弃、不要做**；第 3～8 步是「真的要走项目站」时才有意义。保留本节是因为它同时也是「哪些文件是根绝对的」的可执行地图。

> 顺序不能颠倒：**第 1 步不做，后面所有改动都没有意义**（资源仍在域名根找）。

1. **`nuxt.config.ts`**（**仅项目站**） —— 让 base 可注入、且默认仍是 `/`（用户站/本地开发零影响）：
   - `app.baseURL` 取自环境变量（走项目站时工作流会传 `NUXT_APP_BASE_URL`），默认 `'/'`；
   - 顶部加 `const base = process.env.NUXT_APP_BASE_URL || '/'`，把 `:39` 的 `/favicon.svg`、`:40` 的 `/rss.xml` 写成 `${base}favicon.svg` / `${base}rss.xml`（配置文件里用不了 composable）。
2. **`.github/workflows/deploy.yml`** —— 步骤位置已变（审计时 `:41-45`，当前 `:50-54`）。**现状写法**（用户站，**无需改动**，列出以便对照）：
   ```yaml
   - name: Resolve site url
     id: site
     run: echo "url=https://$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]').github.io" >> "$GITHUB_OUTPUT"

   - name: Generate static site
     run: pnpm generate
     env:
       NUXT_PUBLIC_SITE_URL: ${{ steps.site.outputs.url }}
   ```
   要点：地址由**账号名推导**（用户站固定是 `https://<账号>.github.io`），**不再依赖 Pages 是否已启用**；必须用 `tr` 转小写，因为 Actions 表达式没有 `toLowerCase`。

   **若改走项目站**，才需要在 `env` 里补 base（**旧文写的 `steps.pages.outputs.base_path` 已不可用**——`actions/configure-pages` 已移除，output 不存在，`base_path` 必须自己推导）：
   ```yaml
   NUXT_APP_BASE_URL: /${{ github.event.repository.name }}/
   ```
   （形如 `/my-blog`，Nuxt 需要尾斜杠；同时 `shared/site.ts:37` 的兜底值也要跟着改成 `https://<账号>.github.io/${{ github.event.repository.name }}`，否则 sitemap/RSS/canonical 会少一段子路径。注：这条推导写法**没有实测过**，属于按 GitHub Actions 上下文变量推的推理。）
3. **`shared/site.ts:37`** —— **原建议已作废**：原写「去掉写死的域名、兜底留 `http://localhost:3000`」。现状是仓库里就是**真实用户站域名** `https://fiercecoder888.github.io`，且这正是想要的行为（`nuxt.config.ts:26` 用它兜底，本地不设环境变量时也对）。**仅当**改走项目站，才需要把它改成带子路径的地址（`https://<账号>.github.io/<repo>`），以免 sitemap/RSS/canonical 少一段子路径。
4. **`app/configs/apps.ts`** —— 处理 10 个 `icon`（`:24,31,38,46,54,62,70,77,84,92`）与 `to: '/rss.xml'`（`:86`）。推荐在组件层统一拼（`AppDock.vue:16-17` 一处改动即可覆盖全部 Dock 项），避免把 base 混进纯数据配置。
5. **`app/components/desktop/LaunchpadOverlay.vue`** —— 处理 11 个 `src`（`:76-86`）与 2 个 link 型 `to`（`:85,89`）；同样在这一处统一拼 base。
6. **`app/configs/wallpapers.ts`（配合 `app/composables/useWallpaper.ts`）** —— 处理 8 条 `/wallpapers/...`。注意它是**内联样式**，必须在下发 `background` 时拼 base，不能指望构建期改写。
7. **`/rss.xml` `/sitemap.xml` `/llms.txt` 的 21 处硬链接** —— 涉及 `app/layouts/default.vue:21,23,25`、`app/pages/index.vue:28,31,34`、`app/components/desktop/AboutWindow.vue:48,49,50`、`app/components/desktop/FinderWindow.vue:140,141,142`、`app/components/desktop/TopBar.vue:299,300,301,314,315`（在 `openExternal` 内部统一拼，一处覆盖 5 条）、`app/configs/apps.ts:86`、`app/components/desktop/LaunchpadOverlay.vue:85,89`、`nuxt.config.ts:40`。统一用 `joinURL(useRuntimeConfig().app.baseURL, '/rss.xml')`；**建议保留 `<a :href>` / `window.open` 形态**，不要换成 `<NuxtLink>`——这三者是静态文件不是页面路由，交给客户端 router 反而容易走岔。
8. **`app/components/desktop/AboutWindow.vue:45,46,47`** —— 这三条是**站内页面路由**（`/blog` `/tags` `/about`），把裸 `<a>` 换成 `<NuxtLink>` 即可（这几条**已经确认会被漏**，务必单独立项）。
9. **`app/components/BlogComments.vue:157,180`** —— 静态托管降级（与 base 无关，用户站也一样需要）。**已完成**：降级 UI 已实现在 `BlogComments.vue`（`:20-22` 说明块、`:164-165` 对 404/405 的判定），本条现在只剩「记录」价值。
10. **改完后的验证清单**（不跑 `pnpm generate` 的话，至少人工核对这几条）：
    - `dist/_nuxt/*.css` 里光标仍是 `url(../cursors/...)`（**不应**变成 `/cursors/...` 或 `/my-blog/cursors/...`）——这一条与站点形态无关，**当前用户站下同样成立**；
    - 以下三条是**项目站专属**的期望值。用户站下应反过来核对：`dist/index.html` 的 `_nuxt` 资源、`favicon`、`rss` 三处**不带**任何子路径前缀；页面里 `<img src>` 与菜单栏链接仍以 `/` 开头；`dist/sitemap.xml` / `rss.xml` / `llms.txt` 里的绝对地址是 `https://fiercecoder888.github.io/…`（域名小写、无子路径）。
      - （项目站）`dist/index.html` 的 `_nuxt` 资源、`favicon`、`rss` 三处都带上了 `/my-blog` 前缀；
      - （项目站）页面里 `<img src>` 与菜单栏链接都带 `/my-blog`；
      - （项目站）`dist/sitemap.xml` / `rss.xml` / `llms.txt` 里的绝对地址与部署地址一致。

## 6. 诚实标注（边界与不确定性）

- 本文所有**行号**均来自对**审计当时**工作区（`git HEAD = ad88041`，工作区干净）的真实 grep/read，不是推测。**注**：审计之后 `HEAD` 已到 `8787cc3` 且工作区有未提交改动，部分行号已漂移，引用前请重新确认。
- 「会坏」的判定对 `wallpapers`、`app-icons`、`/rss.xml` 等类别有**构建产物实测**支撑（§4.1，`dist/` 是 base `/` 的产物，这些字符串原样保留即证明它们不受 base 改写机制影响）。**注**：这些判定的前提是**项目站**；用户站下它们不构成问题。
- **未实测**：把 base 设成 `/repo/` 后重新生成的产物。§4.6 中「配好 `app.baseURL` 后 `_nuxt/` 资源自动带前缀」属**基于 Nuxt 源码的推理**（`nuxt/dist/index.js:3207-3213`），建议在 CI 上按项目站真跑一次确认。
- 按任务约束，**本次审计没有运行任何构建命令**（`pnpm generate` / `build` / `install`），也没有改动任何现有代码；本文是唯一新增文件。
- 与既有文档的关系：`docs/部署.md:70` 把 10 条光标 `url('/cursors/...')` 判为会坏，**该条已由构建产物证伪**（§4.2），建议把 `docs/部署.md` 那一行改为「经构建期改写为相对路径，无需处理」；`docs/部署.md:74-79` 的其余清单与本文一致，但**漏了 `AboutWindow.vue:45-47`** 这三条裸 `<a>` 页面链接。（`docs/部署.md` 与 `docs/静态产物验证.md` 正在被并行修订，上面这两处行号可能已漂移。）

### 6.1 本次「状态更新」修订的诚实边界

- 本次修订**只改文档、不改代码、不跑构建**，也**没有重新做一遍全量审计**。
- 修订中**实际重新核实过**的只有这几条：`shared/site.ts:37` 的值确为 `https://fiercecoder888.github.io`；`nuxt.config.ts:26` 确为 `process.env.NUXT_PUBLIC_SITE_URL || SITE.url`；`nuxt.config.ts:39,40,62` 的行号与写法未变；`.github/workflows/deploy.yml` 现为 82 行、含 `Resolve site url` 步骤且**不含** `actions/configure-pages`；`app/components/BlogComments.vue` 的静态降级确已实现（`:20-22`、`:133`、`:164-165`）；`git remote` 的仓库名确为 `Fiercecoder888.github.io`。
- **未重新核实**：§4.2 那段「Vite/Nuxt 把 CSS `url()` 改写成相对路径」的机制分析（只确认了 `dist/_nuxt/entry.Bv2SmSWs.css` 文件仍在，没有重读其内容）；§4.6 关于 `_nuxt/` 资源自动带 base 的推理；以及 §2 清单里除 `shared/site.ts:37`、`.github/workflows/deploy.yml` 之外各条的行号与片段。这些**保持原文结论**，不因为本次修订而升级为「已验证」。
- §5 里新给的 `NUXT_APP_BASE_URL: /${{ github.event.repository.name }}/` 与配套的 `shared/site.ts` 兜底改法，是**按 GitHub Actions 上下文变量推的推理，未实测**（因为本站没有走项目站）。
