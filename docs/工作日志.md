# 工作日志

这个博客兼当**每天的工作日志**：一天一条 `content/worklog/YYYY-MM-DD.md`，内容由 `scripts/new-log.mjs` 从当天的 git 提交自动生成草稿，你只需要补「踩的坑 / 今天学到」。

「每天花 20 分钟回忆今天干了啥」是这类习惯最常见的死因，所以这个流程的目标是：**草稿里的东西不用你回忆，git 已经替你写好了；你只补 git 里没有的那部分。**

- 草稿目录：`drafts/`（与 `content/` 平级，**已被 `.gitignore` 忽略**）
- 发布位置：`content/worklog/<日期>.md`
- 脚本：`scripts/new-log.mjs`（等价命令 `pnpm new:log`）
- schema 定义：`content.config.ts` 里的 `worklog` 集合

---

## 1. 每天怎么用

四步，收工前 5 分钟搞定：

```powershell
cd my-blog

# 1) 生成今天的草稿（默认就是本机时区的今天）→ drafts\2026-09-10.md
pnpm new:log

# 2) 打开草稿，补三件事：summary（一句话总结）、坑与解决、今天学到
notepad drafts\2026-09-10.md      # 换成你惯用的编辑器即可

# 3) 发布：自动移动到 content/worklog/ 并把 frontmatter 的 draft 改成 false
pnpm new:log --publish 2026-09-10

# 4) 提交推送（push 之后 CI 自动构建部署）
git add content/worklog/2026-09-10.md
git commit -m "worklog: 2026-09-10"
git push
```

第 3 步做完，草稿就从 `drafts/` 消失了；**不要**再手改产物文件里的 `draft` 字段，那一步脚本已经做掉了。

不带参数时用的日期 = 你机器上今天的日期（本地时区）。晚上 11 点写的日志算今天，不会跑到明天去。

### 命令速查

| 命令 | 作用 |
| --- | --- |
| `pnpm new:log` | 生成今天的草稿 → `drafts/<今天>.md` |
| `pnpm new:log --date 2026-09-09` | 补写某一天（按那一天的时间窗重新采集 git） |
| `pnpm new:log --force` | 草稿已存在时覆盖（会丢掉已写的内容，慎用） |
| `pnpm new:log --dry-run` | 只在屏幕上打印草稿，不写文件 |
| `pnpm new:log --no-sessions` | 生成草稿时**跳过 Agent 会话采集**，只用 git（第 8 节） |
| `node scripts/agent-sessions.mjs` | 单独看今天的 Agent 会话信号（排查「怎么没采集到」） |
| `pnpm new:log --publish 2026-09-10` | 发布：`drafts/2026-09-10.md` → `content/worklog/2026-09-10.md`，并改 `draft: false` |
| `pnpm new:log --publish --force` | 目标文件已存在时强制覆盖（例如已发布过、改完想重发） |
| `pnpm check:drafts` | 守卫：扫 `content/**/*.md`，发现 `draft: true` 就报错（CI 也会跑） |

行为约定（都是实测过的）：

- 草稿已存在且没带 `--force`：**不覆盖**，打印提示，**退出码 0**（这不算错误）；
- 当天没有提交：**正常生成一份空骨架**并打印提示，退出码 0；
- `--publish` 目标已存在且没带 `--force`：**拒绝**，退出码 1（`drafts/` 里的草稿原封不动）；
- `--publish` 时如果 `title` / `summary` 还是占位、或 `pitfalls` / `learned` 还是空的，会打印 `⚠` 提醒但**照样发布**（`title` 为空则直接拒绝，因为空的 `title` 会被 schema 挡下来、让构建失败）；
- 本脚本必须**在仓库根目录**运行（路径按仓库根算，跑错目录会直接报错退出）。

---

## 2. 草稿长什么样

`scripts/new-log.mjs` 生成的文件（下面是 2026-09-10 的真实产物，提交列表中间省略了）：

```markdown
---
title: 桌面 OS 风格个人博客（Nuxt 4 + Nuxt Content + macOS 复刻）
date: '2026-09-10'
summary: （一句话总结今天）
tags: []
agents: [dsh, codex]        # 今天有哪些 Agent 参与（自动填，见第 8 节）
# project: my-blog         # 可选：项目名，方便以后按项目聚合
pitfalls: []
# pitfalls:                # 填坑时改成这个形状（agent 表示这条坑是谁踩的）
#   - problem: （现象）
#     solution: （怎么解决的）
#     time: 约 30 分钟       # 可选
#     agent: 'dsh'          # 可选：dsh / codex / claude / 你
learned: []
# mood: 4                  # 可选：心情 1-5
draft: true
---

<!-- 由 node scripts/new-log.mjs 生成：git 原料已填好，只差「踩坑 / 学到」。
     补完后发布：node scripts/new-log.mjs --publish 2026-09-10 -->

## 今天做了什么

<!-- 下面这几行是从 git 提交里**按权重挑出来**的：只有 feat/fix/perf/refactor 单独成行，
     最多 5 行；chore/docs/test 这类低权重提交已折叠成下面那行摘要（原文没删，跑 git log 还能看到）。
     想看某个提交改了哪些文件：git show --stat <hash> -->

- 18:57 `09754ac` feat: 桌面 OS 风格个人博客（Nuxt 4 + Nuxt Content + macOS 复刻）（+18986 / -0，145 个文件）
- 19:45 `04b49ed` fix(ci): 站点地址转小写，补静态托管验收工具（+311 / -8，8 个文件）
- 20:30 `668cf74` fix(blog): 文章页不再把「内容库未就绪」当成 404（线上 15 秒假 404）（+1521 / -94，15 个文件）
- 20:45 `b0690e0` fix(blog): 让文章页 useAsyncData 的 key 与路径形态无关（尾斜杠导致 payload 永远不命中）（+33 / -10，2 个文件）
- 23:22 `228da9e` feat(worklog): 工作日志 + 「无限进步」栏目；草稿安全防护；个人信息系统接线（+3389 / -51，49 个文件）
- （另有 13 个低权重提交：docs ×2、ci ×3、chore ×7、tools ×1，已折叠 —— 要看细节跑 git log）

主要改动目录：`public/` 67 个文件、`app/` 58 个文件、`(根目录)` 46 个文件、`tools/` 26 个文件、`docs/` 18 个文件
全天合计：18 个提交，184 个文件（同一文件反复改只算一次；实际改动记录 251 条），+25864 / -231，其中 77 个二进制文件（不计行数）

## 踩的坑与解决

<!-- 把 frontmatter 里的 pitfalls 在这里展开写（problem / solution / 耗时 / agent）；
     结构化的那份留在 frontmatter 里，以后「坑库」要靠它聚合，散在正文里就永远是死数据。 -->
<!-- 每条坑请补上 agent（dsh / codex / claude / 你）；会话记录显示今天参与：dsh、codex -->

## 今天学到

<!-- 一句话一条，同步进 frontmatter 的 learned -->

## 今天的会话线索（自动采集，可删）

<!-- 以下内容由 scripts/agent-sessions.mjs 从本机会话记录里采集：**只有元数据与截断摘要**，
     正文从未被写入。这是**候选**，不是事实陈述 —— 发布前请删掉整节，或改写后再留。 -->

- **dsh** 10:12–12:40（约 148 分钟，工具调用 214 次，含 3 个子代理会话，有代码改动）
  - 需求（截断）：把工作日志的草稿流程做成脚手架，从当天 git 提交自动生成…
  - 结论（截断）：脚手架三个子命令都跑通了，草稿默认落在 drafts/…（摘要最多 120 字）
- **codex** 14:03–15:22（约 79 分钟，工具调用 61 次，有代码改动）
  - 需求（截断）：帮我看看线上文章页为什么 15 秒后假 404…
  - 结论（截断）：根因是 useAsyncData 的 key 跟路径形态绑死…
```

（`agents` 那行、「今天做了什么」的挑行与折叠摘要、统计口径都是脚本真实产出的形状，数字取自 2026-09-10 那天的 `git log`；「今天的会话线索」里那两条是为了展示格式编的占位 —— 本机那天没有属于这个仓库的会话记录。）

`title` 的初始值是脚本从当天提交里挑的（优先 `feat` / `fix`，同档取标题最长的那个），会被剥掉 `feat:` `fix(blog):` 这类前缀；当天没有提交时是 `（待填）`。其余备选标题放在正文的一条 HTML 注释里，方便你换。

### frontmatter 契约（与 `content.config.ts` 的 `worklog` schema 对齐）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `title` | 字符串 | **必填、不能空**。空了构建会被 schema 拒绝 |
| `date` | 字符串 | `'YYYY-MM-DD'`，与文件名一致 |
| `summary` | 字符串 | **必填**。一句话总结 |
| `tags` | 字符串数组 | 例如 `[部署, 排障]` |
| `agents` | 字符串数组 | 今天有哪些 Agent 参与，例如 `[dsh, codex]`。由脚本自动填（见第 8 节） |
| `project` | 字符串 | 可选。草稿里以 `# project:` 注释形式待命，要填就取消注释 |
| `pitfalls` | 对象数组 | `- problem` / `solution` / `time`（可选）/ `agent`（可选，这条坑是谁踩的） |
| `learned` | 字符串数组 | 一句话一条 |
| `mood` | 数字 1–5 | 可选。草稿里以 `# mood:` 注释形式待命 |
| `draft` | 布尔 | 草稿 `true`，发布后由脚本改成 `false` |

`pitfalls` / `learned` 为空时写 `[]`（不是留空），这样 schema 和以后的聚合脚本都好处理。

`agents` 是**日志级**（今天谁参与了），`pitfalls[].agent` 是**单条坑级**（这条坑是谁踩的）—— 两种粒度都要，因为「今天 dsh 参与了」和「这个坑是 codex 踩的」是两件事。两者都在 `content.config.ts` 的 `worklog` schema 里。

---

## 3. 为什么草稿必须放 `drafts/`，而不能放 `content/` 里写 `draft: true`

这是**实测结论，不是推测**。

`@nuxt/content` 的 `draft` 字段**只挡得住查询，挡不住构建**：

- 页面、列表、sitemap、RSS、llms.txt 这些查询都带 `.where('draft', '=', false)`，所以草稿不会出现在它们里面 —— 看起来是安全的；
- 但 `nuxt generate` 会把**整个内容库**导出成 `/_nuxt_content/blog/sql_dump.txt`（SQLite 的原始导出，gzip + base64），部署到 GitHub Pages 后**任何人匿名访问都能下载**；
- `@nuxt/content` 构建期**没有任何按 `draft` 字段过滤数据库行的逻辑**。实测：临时往 `content/` 里放一篇带唯一标记的 `draft: true` 文章，重新构建后 dump 长度从 **29763 → 30618 字符**，那个标记在 dump 里出现 **3 次**；删掉后恢复原样。

也就是说：**只要 `draft: true` 的文章还在 `content/` 里，它的正文就已经公开了**，`draft: true` 只给了你一种"没发布"的错觉。

另外，`sql_dump.txt` **不能直接删掉**：客户端的内容库（Spotlight 搜索、Finder、终端 `ls`）就靠 `fetchDatabase()` 加载它，删了这些功能全废。

结论：**草稿必须待在 `content/` 之外**，也就是 `drafts/`。工作日志的 `draft` 字段只是为了与 `blog` 集合同构，以及给发布脚本一个明确的开关，**不用它当隐藏开关**。

---

## 4. 草稿的存放与备份（**已配好私有备份**）

草稿放在 `drafts/`，而 `drafts/` 被主仓库 `.gitignore` 忽略。这一步解决的是「不进构建、不泄露」：

| 方案 | 好处 | 代价 |
| --- | --- | --- |
| 草稿放 `drafts/`（已 gitignore） | 永远不会进构建、不会出现在 dump / sitemap / RSS / 网站里 | 单看这一条：只存在本机，硬盘坏了 / 误删就没了 |
| 草稿提交进**主**仓库 | 有远程备份 | **主仓库是 public**：虽然不上网站，但任何人 clone 它、或读 raw 文件就能看到原文 —— 和公开没有实质区别 |

**所以最后的做法是：`drafts/` 本身就是一个独立的、私有的 git 仓库。**

- 目录：`E:\...\my-blog\drafts`（主仓库忽略它，它自己管自己）
- remote：**私有**仓库 `https://github.com/Fiercecoder888/my-blog-drafts`
- 好处：既「不公开」（不在 public 主仓库里），又「有备份」（推到了私有远程）

**备份是自动的**：`scripts/new-log.mjs` 在两步会顺手 `commit + push` 到那个私有仓库 ——
生成草稿后记 `draft: new <日期>`，发布之后记 `draft: publish <日期>`。
推送失败（没网、凭据失效）**只警告不中断**，草稿已经落盘，稍后补一次 `git -C drafts push` 即可。

手改草稿后想立刻备份：

```powershell
git -C drafts add -A
git -C drafts commit -m "draft: 手改内容"
git -C drafts push
```

> ⚠️ 仍然要说清楚：**正式发布进 `content/` 的日志是给全世界看的** —— 它会出现在网站、sitemap、RSS、llms.txt，也会出现在公开的 `sql_dump.txt` 里。别把公司项目名、内部地址、密钥、同事姓名写进去。草稿阶段随便写，**发布前请过一遍**。

---

## 5. `pitfalls` 为什么要写成结构化的

因为 frontmatter 里的字段是**数据**，正文里的一段文字只是**文本**。

「无限进步」栏目以后要按标签把坑聚合成「坑库」（同一个坑栽过几次、哪类坑最耗时、哪个项目最费劲）—— 这些都要求 `problem` / `solution` / `time` 是能查询、能聚合的字段。写在正文里，聚合脚本只能靠正则去猜，等于永远是死数据。

所以规矩是：

- **结构化那份留在 frontmatter**：`pitfalls`（problem / solution / time）和 `learned`（一句话一条）是唯一权威；
- **正文那份是给人读的**：在「踩的坑与解决」下面把它们展开写，可以加背景、加代码、加截图；
- 以后回头看日志，看正文；以后做「坑库」页面，读 frontmatter。

---

## 6. 常见问题

**当天没有提交（今天没写代码 / 只改了别的仓库）**

脚本照样生成一份空骨架，正文里写「（2026-09-10 没有 git 提交）」，退出码 0。你可以：
- 手写「今天做了什么」（会议、调研、读文档也算工作）；
- 或者干脆跳过今天 —— 日志断一天不会怎样，**别为了凑数把习惯搞死**。

**要补写前几天**

```powershell
pnpm new:log --date 2026-09-07
```

日期按本机时区解释，git 是拿那一天的时间窗重新查的（前一天 0 点到后一天 23:59 全查出来、再按**你本机时区的日期**精确归类），所以补写和当天写的结果一致。

**误发布了怎么撤回**

把文件**移回** `drafts/`，并把 `draft` 改回 `true`：

```powershell
git mv content\worklog\2026-09-10.md drafts\2026-09-10.md   # 或者手动移动
# 然后编辑 drafts\2026-09-10.md，把 draft: false 改回 draft: true
git commit -m "worklog: 撤回 2026-09-10 草稿"
git push
```

注意：**已经 push 过就意味着正文已经公开过**（而且在 git 历史里）；如果里面误写了敏感信息，移回来只是止血，还得单独处理历史（改内容重新提交、或和托管平台协商）。这也是为什么发布前要过一眼。

**忘了跑守卫脚本？**

`.github/workflows/deploy.yml` 在 `Generate static site` 之前会跑 `pnpm check:drafts`，发现 `content/` 里有 `draft: true` 就报错退出，构建直接被拦下。

**某天的草稿想推倒重来**

`pnpm new:log --date <日期> --force`（会覆盖，先想清楚里面有没有已经写好的内容）。

**一个提交都不想漏**

脚本会把当天的提交**按权重挑出最多 5 行**（只有 `feat/fix/perf/refactor` 单独成行），其余折叠成末尾一行摘要 —— 想一条不漏地看，直接 `git log`（见 8.7）。

---

## 7. 采集口径（脚本到底怎么算的）

只有一次 `git log` 调用（`--pretty` + `--numstat` 一次性拿到 hash / 时间 / 标题 / 父提交 / 逐文件增删），不为每个提交单独调一次 git。

- **时区**：日期和时间一律按**本机时区**算（`--date=format-local:`）。查询窗口左右各放宽一天，再在 JS 里按「本机时区的日期」精确过滤，所以晚上 23:50 的提交算今天，不会漂到明天。
- **多作者 / 不同时区的提交**：按该提交在本机时区显示的日期归类（不是提交自身的 `+0800` 偏移），与你 `git log` 里看到的默认观感一致。
- **合并提交（merge）**：会列出来，但标成「合并提交，git 不给单文件统计」—— git 默认不为 merge 输出 diff，所以它的文件数是 0，这是 git 的正常行为，不是漏采。合并本身带来的改动，算在它两侧的普通提交里。
- **根提交**：加了 `--root`，所以仓库第一个提交的文件清单和增删行数也会被统计到（否则 git 默认不给根提交输出 diff）。
- **二进制文件**：`public/` 下的图片、字体这类会显示成「N 个二进制文件（不计行数）」，它们的增删行数按 0 计，但文件数照算。
- **重命名**：路径按重命名后的新路径归类（`old => new` 会归到 `new` 所在的目录）。
- **空提交**：显示「（无文件改动）」。
- **目录聚合**：按顶层目录统计改动文件数，取 Top 5；仓库根目录下的散文件（如 `package.json`）归到 `(根目录)`。

脚本是纯 Node 实现、零第三方依赖，也不写任何 git 钩子，`drafts/` 不存在时自动创建。

---

## 8. Agent 采集（谁干的活，从哪来）

前面几节解决的是「git 里有的东西自动进草稿」。这一节解决「git 里没有的东西」——**是哪个 Agent 干的**，以及**它当时到底在干嘛**。三个 Agent 都会把会话记录留在本机，脚本顺手读一遍，压成极简信号。

### 8.1 三个适配器：位置与格式

`scripts/agent-sessions.mjs` 里三个适配器，纯 Node、零依赖（zstd 用 Node 24 内置的 `zlib.zstdDecompressSync`，**不装任何包**）：

| Agent | 位置 | 格式 |
| --- | --- | --- |
| Codex CLI | `~/.codex/sessions/<YYYY>/<MM>/<DD>/rollout-*.jsonl` | 明文 JSONL，`{timestamp, type, payload}`。`session_meta.payload` 有 `cwd` / `session_id` / `originator`；`turn_context.payload` 有 `model`；`event_msg/user_message` 是首条需求；`event_msg/agent_message`（`phase: final_answer`）是结论 |
| Claude Code | `~/.claude/projects/<路径变形>/<sessionId>.jsonl` | 明文 JSONL，`{type, sessionId, ...}`。`user` / `assistant` 记录带 `cwd` 与 `timestamp`；`ai-title` 是一句话标题；`system/turn_duration` 有单轮耗时 |
| DSH | `~/.dsh/sessions/<路径变形>/<会话id>/session.jsonl.zstd` | **zstd 多帧**压缩的 JSONL（一帧一条记录）。第 1 帧是会话头 `{type:'session', createdAt, cwd, agentPreset, delegationDepth, origin}`，之后是 `text-chunks` / `tool/call` / `turn/start` 等事件流 |

「路径变形」= 非 ASCII 字符被替换成 `-`，例如 `E:\AIStudy\...\Work09098` → `--E-AIStudy-AIProjects-factory-WorkLog-Work0909-Work09098--`。所以判断会话属于哪个仓库时，**优先用记录里的 `cwd` 字段**（大小写、尾斜杠差异都容忍），目录名只在 cwd 缺失时才退化使用。

> ⚠️ 这个仓库是**嵌在工作区里**的：仓库根是 `...\Work09098\my-blog`，而 Agent 通常在外面那层 `...\Work09098` 里启动。所以默认把**父目录**也算「相关」，否则本机上真正在改这个博客的会话一条都采不到（实测过）。想收紧成只认仓库根，用 `--exact`。

### 8.2 只提取什么、绝不提取什么（隐私边界）

**会话原文里可能有公司代码、内部路径、密钥、客户信息。** 所以采集器只拿下面这些，其余一律在读出时丢弃：

**会被提取的（进草稿的全部内容就是这些）**

- 会话开始 / 结束时间（本机时区）、时长
- `cwd`、会话 id（**截断成 12 位**）、模型 / `agentPreset`
- **首条用户需求的 ≤80 字摘要**（单行化 + 截断）
- **最后一条助手结论的 ≤120 字摘要**（同上）
- 工具调用**次数**、失败/重试**次数**、有没有落盘类工具调用、子代理会话个数

**绝不提取、读到即丢**

- 完整对话、思考链（`thinking`）、工具入参/出参、命令输出、文件内容、附件、图片
- 这些不会被写进日志，也不会被写进任何中间文件；代码里的 `discard()` 空函数就是显式标记「原文到此为止」，防止后来者顺手把 body 传出去

另外：

- 采集**只在本机跑**，不联网、不发任何请求；
- 产出先落在 `drafts/`（**私有**草稿仓库），发布前你自己再过一遍；
- 草稿里那一节标题写的是「今天的会话线索（自动采集，可删）」，并且明确标注**这是候选、不是事实陈述** —— 发布前请删掉整节或改写后再留。

### 8.3 噪音预算（硬性上限）

会话记录是**一天几十条**的量级，直接铺开就是垃圾场。所以：

| 规则 | 值 |
| --- | --- |
| 每个 Agent 每天最多 | **3 条**信号 |
| 跨 Agent 合计最多 | **5 条**信号 |
| 排序依据 | 有代码改动 > 有失败重试 > 时长（再叠一点同分裁决：根会话 > 子代理会话） |

两条额外的降噪：

1. **子代理会话合并进根会话**：一次「派 3 个子代理」的任务，原始记录里是 4 个会话，进日志只算 1 条（那条上标「含 N 个子代理会话」）。没有根会话时，子代理们合成一条汇总。
2. **超预算的直接丢弃**，只在 CLI 输出里说明丢了几条、为什么丢；草稿里不会出现「被丢掉的会话」清单。

### 8.4 `agents` 与 `pitfall.agent` 怎么来的

草稿 frontmatter 的 `agents` 由 `scripts/new-log.mjs` 合并两个来源，**优先级明确**：

1. **提交里的 trailer 优先**：解析提交信息（正文，含 `-m` 的第二段）里的 `Agent: dsh` / `agent：codex` 这类行，大小写不敏感、允许缩进、一行可写多个（`Agent: dsh, codex`）。别名归一：`claude-code` → `claude`、`deepseek-harness` → `dsh`、`human`/`me` → `你`；未知名字原样保留小写。
2. **没有 trailer 才用会话记录推断**：即 8.1 采集到的会话里出现过的 Agent 名。

两边都没有 → `agents: []`（草稿里会带一行注释告诉你这里是空的）。

`pitfalls[].agent` 是**手填**的（脚本不知道某条坑是谁踩的）：草稿的「踩的坑」区会带一行提示，把今天采集到的参与者列出来给你抄：

```markdown
<!-- 每条坑请补上 agent（dsh / codex / claude / 你）；会话记录显示今天参与：dsh、codex -->
```

### 8.5 某个 Agent 没装怎么办

**自动跳过，不报错、不中断。** 目录不存在 / 文件读不了 / 格式变了，都只是打印一行说明，草稿照常生成：

```text
codex：C:\Users\...\.codex\sessions\2026\09\11 下没有当天会话，已跳过
claude：没有找到 C:\Users\...\.claude\projects（这台机器没装 Claude Code / 或没跑过会话），已跳过
```

不是每个用户都装了这三个 Agent，所以「缺失」是正常状态，不是错误。

### 8.6 自己排查：单跑适配器

```powershell
# 今天的信号（人可读）
node scripts/agent-sessions.mjs

# 指定日期 / 机器可读 / 不做预算看全部候选 / 只认仓库根
node scripts/agent-sessions.mjs --date 2026-05-19
node scripts/agent-sessions.mjs --date 2026-09-11 --json
node scripts/agent-sessions.mjs --all
node scripts/agent-sessions.mjs --exact

# 生成草稿时跳过会话采集，只用 git
node scripts/new-log.mjs --no-sessions
```

`--json` 输出里除了信号，还有 `notes`（每个适配器找到几个会话、耗时多少、为什么跳过）和 `dropped`（超出预算被丢了哪几条、什么原因）—— 「今天怎么没有采集到」基本看这两个字段就够了。

### 8.7 去噪：草稿里「今天做了什么」最多 5 行

用户最在意的是**不要垃圾信息**，所以草稿骨架上对提交也做了同样的处理：

- **权重**：只有 `feat` / `fix` / `perf` / `refactor` 才算「有内容」、才可能单独成行；`docs` / `test` / `build` / `ci` / `chore` / `style` 一律降级。
- **合并同类**：`fixup!` / `squash!` / `wip` / `typo` / `merge` / `revert` / `lint` / `format` 这类提交**不单独成行**，而是并进时间上最近的那条可见提交（显示成缩进的「（并入）…」）。前面没有可见提交时就只计数。
- **上限**：最多 **5 行**，超出按权重（同权重按改动量）截断。
- **折叠摘要**：末尾固定一行说明被折叠了什么，但不展开，例如：

  ```markdown
  - （另有 5 个低权重提交：docs ×2、test ×1、ci ×1、chore ×1；4 个合并/修补提交已并入上一条，已折叠 —— 要看细节跑 git log）
  ```

- **统计口径**：同一文件反复改**只算一次**（全天按唯一路径去重）；行数增删仍按全部改动累计，所以「10 个文件（实际改动记录 15 条）」这种写法会同时给出两个数。

原始提交当然一条都没被删 —— 想回看就跑 `git log`，草稿只是不再把它们铺在你面前。
